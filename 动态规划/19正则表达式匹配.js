/**
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
/**
 * 规则：'.'匹配任意字符,'*'表匹配前一个字符0或多个
 */
 var isMatch = function(s, p) {
    const target = s.split('');
    let dp = p.split('');
    // for(let i =0; i< target.length ; i++){
    //     if(dp[i] === target[i])
    //         continue;
    // }
    // return false;
};